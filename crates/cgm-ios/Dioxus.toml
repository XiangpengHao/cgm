[application]

[bundle]
identifier = "com.aidex.glucose"
publisher = "AiDEX Glucose"
# Permission strings + entitlements the app needs at runtime. dx merges these
# into the generated Info.plist; see README.md if your dx version wires them
# through Xcode instead.

[bundle.ios]
# Usage descriptions (required or iOS rejects the API at runtime).
info_plist_extra = "ios/Info.plist"
entitlements = "ios/cgm.entitlements"

[mobile]
# Renders the shared cgm-ui in a WKWebView, same components as the web build.
