[application]

[web.app]
title = "Glucose — AiDEX X / GX-01S"
# Served at a custom-domain root (https://cgm.xiangpeng.systems/), so assets
# resolve from "/". If you ever serve from a project Pages URL
# (https://<user>.github.io/cgm/) instead, set `base_path = "cgm"`.

[web.resource]
style = []
script = []

[web.resource.dev]
script = []
