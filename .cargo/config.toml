# Web Bluetooth lives behind web-sys's "unstable APIs" gate. dx/cargo builds for
# the web target need this cfg or the Bluetooth bindings don't exist.
[target.wasm32-unknown-unknown]
rustflags = ["--cfg=web_sys_unstable_apis"]
